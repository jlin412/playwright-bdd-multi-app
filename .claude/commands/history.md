# /history

Show the durable history and current state of a feature.

Input `$ARGUMENTS`: `<feature>`.

Do:
1. Print `artifacts/<feature>/history.md` (transitions + improvement notes).
2. Append a one-line **current state** summary derived from the sidecars (same rules as
   `/status`).

Read-only.
