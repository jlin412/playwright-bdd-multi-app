# Project Memory — Review Calibration

Learned reviewer preferences, distilled by `/bootstrap` from `overridden:` rows in the
deliverables' Review History tables. The `qa-review` skill reads this before selecting
review questions: a preference recorded here is **settled** — applied, not re-asked
(recorded as `calibration` in Review History).

Each entry: the preference · evidence (feature + date of the overridden rows) · how the
specialists apply it.

## Preferences

None recorded yet — no recurring `overridden:` review decisions have accumulated.
